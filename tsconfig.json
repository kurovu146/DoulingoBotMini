{
  "compilerOptions": {
    "target": "ES2022",          // Hỗ trợ các tính năng ECMAScript mới nhất
    "module": "NodeNext",        // Đảm bảo sử dụng ESM
    "moduleResolution": "NodeNext", // Giải quyết module theo chuẩn Node.js ESM
    "rootDir": "./src",          // Thư mục chứa file TypeScript
    "outDir": "./dist",          // Nơi lưu trữ file JavaScript đã biên dịch
    "strict": true,              // Bật chế độ kiểm tra nghiêm ngặt
    "esModuleInterop": true,     // Hỗ trợ import CommonJS module
    "skipLibCheck": true,        // Bỏ qua kiểm tra kiểu của file .d.ts
    "allowSyntheticDefaultImports": true, // Cho phép import mặc định từ CommonJS
    "forceConsistentCasingInFileNames": true // Bắt buộc tên file nhất quán
  },
  "include": ["src"],            // Chỉ định thư mục nguồn
  "exclude": ["node_modules"]    // Loại trừ thư mục node_modules
}
